# Web app
Flask>=3.0
requests>=2.31

# Production WSGI server (used by Dockerfile; harmless for local `python app.py`)
gunicorn>=21.2

# Audio / DSP
numpy>=1.24
soundfile>=0.12
librosa>=0.10
praat-parselmouth>=0.4

# Report generation
matplotlib>=3.7
python-docx>=1.1
Pillow>=10.0

# The legacy desktop UI (legacy/factor.py) additionally uses sounddevice + tkinter,
# but those are NOT required by the web app.
